# Dependencies and build output
node_modules
dist

# Local environment
.env
.env.*
!.env.example

# Editor and OS leftovers
.DS_Store
*.log
vite.config.ts.timestamp-*

# Rendered for looking at, not for keeping; the sprites are the source
sprites.png

# The reference recordings the animations were decoded from. They are not ours
# to redistribute, and nothing in the build needs them — see NOTICE.
references/

# Local Cloudflare state (wrangler dev writes its own R2 bucket and KV here)
.wrangler
