# Files that live beside the site but are not part of it. Everything else in
# this directory is published as-is (wrangler.toml uploads it as static assets).
README.md

# What every page shares, before scripts/site-pages.ts copies it into them.
partials/

# Local dev state: created by `wrangler dev` when it is run from this directory,
# and never anything to publish.
.wrangler/
