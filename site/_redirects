# Each page's address is the clean one; the .html spellings are aliases.
/download.html   /download   301
/changelog.html  /changelog  301
/about.html      /about      301

# Where the site's JSON lived before it had an /api/ (the 0.1.0 page read these).
/latest          /api/latest.json    301
/changelog.json  /api/releases.json  301
